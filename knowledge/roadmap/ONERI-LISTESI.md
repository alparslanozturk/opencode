# opencode — Geliştirme Öneri Listesi (tut listesi)

> Repo kopyası — kaynak: Patron'un `/root/.openclaw/workspace/queue/opencode-ONERI-LISTESI.md`
> tut listesi (Alp offline, tek kanal git). B5/B1 uyarınca buraya taşındı; A/B bölümleri
> kaynakla birebir aynıdır (ID, sütun, ifade değişmez), C bölümünü Doktor doldurmuştur.
> İlgili: `knowledge/roadmap/README.md`, `knowledge/architecture/decisions/0001-v1-scope-and-guardrails.md`.

Kaynak: Alp, 2026-09-22 19:44 — *"Ekran görüntülerini inceleyerek geliştirme önermesini bir tut listesi yap…
Orada önerilerini yaz, Doktor'a söyle önerilerini yazsın. Ondan sonra yavaş yavaş yaparız."*

Kural: her madde **tek satır başlık + kaynak + etki/zorluk**; durum sütunu `öneri → onaylı → yapıldı`.
Bu dosya **maskeleme kuralına uyar**: kurum hostname/IP/URL yazılmaz (yerine `<kurum-host>`, `<kurum-uç>`).

## A) Saha kullanımından çıkan öneriler (ekran görüntüleri, 2026-09-22)

| # | Öneri | Kaynak | Etki | Zorluk | Durum |
|---|---|---|---|---|---|
| A1 | `kontrol` çıktısı kurum host/port'unu **ekrana tam basıyor** → maskeleme modu (`--gizle` ya da otomatik `<kurum-host>:443`) | 19:06/16:41 ekranı | Yüksek (ekran → Telegram) | Düşük | öneri |
| A2 | İki uçlu modda rapor **33 satıra** çıkıyor → ≤29 satıra indir (bilgi kaybı yok) | 16:36 rapor | Orta | Düşük | öneri |
| A3 | `kontrol` son hata satırını **kısaltmasız** ve net göstersin; `--ayrintili` ile log kesiti | 19:06 raporu | Orta | Düşük | öneri |
| A4 | Derlenmiş ikili için **duman testi** zaten eklendi (`script/smoke-ikili.sh`) → `kur.sh derle` sonunda **opsiyonel** çağrı + uyarı | olay kaydı 1.0.1 | Yüksek | Düşük | öneri |
| A5 | `kur.sh` sürüm/derleme **izlenebilirliği**: `kontrol` ekranında `sürüm + commit + derleme zamanı` | denetim bulgusu 3 | Orta | Düşük | öneri (T4) |
| A6 | İlk açılışta `/models` seçimi **kalıcı** olsun (config'e yazılsın), sonraki açılışta sorulmasın | saha ilk kurulum | Orta | Düşük | öneri |
| A7 | TUI oturum adları okunabilir olsun (`ses_f362…` yerine `ansible: baseline` gibi) + `/sessions` başlıkları | 19:07/19:35 + **09:00** footer | Düşük | Orta | öneri |
| A8 | `AGENTS.md` **proje rehberi şablonu** kuralım içersin (onay kapıları, envanter yeri, sabitler) → sahada tek komutla üretilsin | 19:41 Alp kuralı | Yüksek | Orta | öneri |
| A9 | Tehlikeli komut kalıpları için **(belge) politika bloğu**: canlıya `--check`'siz playbook, `rm -rf`, `systemctl restart` | 19:41 kuralı | Yüksek | Orta | öneri (T7) |
| A10 | Uzun yanıtlarda **context uyarısı + otomatik özet** (belirli eşikte) ve `session export` kolaylığı | 19:35 (40K/%15), 19:41 (44K/%17), 09:00 (134K/%51), **11:42 (167.8K/%64)** | Orta | Orta | öneri |
| A11 | Offline kurulum için **npm proxy/ağ ön kontrolü** (`kontrol` içinde uyarı satırı) | 1.0.1 saha kurulumu | Orta | Düşük | öneri |
| A12 | `output` limiti uçtan öğrenilsin (şu an şablon 4096 sabit) | kontrolde `baglam` satırı | Orta | Orta | öneri (T3) |
| A13 | `bin/opencode.tar.xz` gitignore'a alınsın / `bin/` düzeni netleşsin | denetim bulgusu 5 | Düşük | Düşük | öneri (T5) |
| A14 | main'e **CI**: `bash -n` + `shellcheck` + duman testi (ucuz, çevrimdışı) | denetim bulgusu 7 | Orta | Orta | öneri (T5) |
| A15 | `bash` izinlerinin **bileşik komutla aşılması** (`ls; rm -rf …`) engellensin | denetim bulgusu 1 | Yüksek | Orta | öneri (T5/T7) |
| A16 | `KURUM_KEY`'in `ps` çıktısına düşmesi engellensin (dosyadan okuma/`env -i`) | denetim bulgusu 4 | Yüksek | Düşük | öneri (T5) |
| A17 | **Saha sabitleri** (AD/NS sunucu IP'leri, NTP havuzu, satellite `pub` yolu) kurulum sırasında `AGENTS.md`'ye şablon satır olarak **yazılsın** → her oturumda Alp'in tekrar anlatması bitsin (B4/A8 ile birleşik; maskeleme uygulanır) | 09:00 ekranı | Yüksek | Düşük | öneri |
| A18 | Model **eksik bilgi yüzünden işi durdurup bekliyor** (ör. bir host'un IP'si çözülemedi → "IP verir misin, yoksa host dosyasından çıkarayım mı?" diye sorup durdu) → **bloke etme politikası**: çözülemeyen öğeyi `eksikler.md`/TODO'ya yaz, kalanlarla devam et, tur sonunda "açık kalanlar" listesi ver | 09:00 ekranı | Yüksek | Orta | öneri |
| A19 | **Değişiklik kapısı (CN) kuralı** modele anlatıldı ve model kendi kuralını üretti ("canlı envanterde playbook = değişiklik = CN zorunlu; SSH ile salt-okunur kontrol serbest; **yeni kurulan makineler CN'siz**") → bu kural `AGENTS.md` **politika bloğuna** yazılsın (A8/A9 kapsamı) | 11:42 ekranı | Yüksek | Düşük | öneri |
| A20 | ⚠️ **Kural belgede kaldı, teknik kapı yok:** model **CN numarası olmadan** `03-ipa` (IPA enroll = gerçek değişiklik) playbook'unu çalıştırmış → değişiklik kapısı **zorlanmalı** (izin bloğu/hook; T7/Faz 0). Kanıt: modelin kendi itirafı ("CN numarası henüz verilmedi") + 12:07 durum tablosunda `03-ipa = Enroll başarılı`. Kapı **tutarsız**: sep kurulumları için izin soruyor ama IPA enroll'u CN'siz koşmuş | 11:42 + **12:07** ekranı | **Çok yüksek** | Orta-Yüksek | öneri (T7) |
| A21 | **TUI glifleri saha terminalinde kutu/`?` olarak çıkıyor** (animasyonlu spinner/pulse = Unicode braille/blok karakterler; MobaXterm fontu + sunucu locale UTF-8 değilse "kutu içinde soru işareti"). Kanıt: MobaXterm font penceresinde yalnız Consolas/Courier/DotumChe/Fixedsys (braille yok) → fallback yok, kutu kalır. **13:15–13:18:** Alp'in terminalinde **metin ve Türkçe karakterler düzeldi**; kalan 4 kutu yalnız **animasyon glifleri** bölgesinde (`Build` öncesi ×2, ilerleme çubuğu yanı ×2) → tam sıfırlama için `animations_enabled=false` ya da tam Unicode font gerekir. Çözüm paketi: (a) `kur.sh`/doküman **locale kontrolü** (`LANG`/`LC_ALL` = `*.UTF-8`, değilse uyar/öner), (b) MobaXterm **font+charset UTF-8** notu, (c) TUI'de `Ctrl+P` → **"Disable animations"** (`animations_enabled=false`; kodda fallback zaten `[⋯]` — yani az ama sıfır değil: metin de ASCII-güvenli olabilir) | 12:20 + **12:27/13:18** ekranı | Orta | Düşük-Orta | öneri |
| A22 | ⚠️ **Kopyalama sessizce başarısız, toast yanıltıcı:** seçim + kopyalamada "Copied to clipboard" bildirimi çıkıyor ama **panoda bir şey yok**. Neden (kod teyitli): `tui/src/clipboard.ts` `write()` = **OSC 52** (MobaXterm desteklemiyor) + `xclip/xsel/wl-copy` (sunucuda yok, DISPLAY yok) + `clipboardy`; **tüm hatalar `.catch(() => undefined)` ile yutuluyor** → `selection.ts` toast'ı koşulsuz "başarılı" gösteriyor. Öneri: (1) gerçek sonucu bildir (başarısızsa **uyar**), (2) `tui.json`'da `{"mouse": false}` belgelensin → TUI fareyi bırakır, seçimi terminal yapar ve **MobaXterm copy-on-select** ile Windows panosuna gider | 12:20 ekranı (+isim Alp) | Yüksek | Düşük-Orta | öneri |

> **09:00 (2026-09-23) eki:** A7 ve A10'a yeni kanıt satırı işlendi (footer adı hâlâ `ses_…`; context 134K/%51). İki yeni madde: **A17** (saha sabitleri `AGENTS.md`'ye) ve **A18** (modelin bloklaması yerine devam politikası).
>
> **11:42 (2026-09-23) eki:** A10'a yeni kanıt (167.8K/%64). İki yeni madde: **A19** (değişiklik kapısı/CN kuralı `AGENTS.md` politika bloğuna) ve **A20** (kural belgede kaldı → `03-ipa` playbook'u **CN'siz** çalıştırılmış; teknik zorlama şart).
>
> **12:07 (2026-09-23) eki:** Durum tablosu ekranı (01-ping ✓ · 01-ntp "doğru" · 02-subs "SSL expired → re-registration" · **03-ipa "Enroll başarılı"** · 04-dnf "repo ✓, reboot gerekir" · 05/06-sep "Bekliyor"; model "sep kurulumlarını deneyelim mi?" diye soruyor). A20'ye kanıt işlendi (CN'siz enroll + tutarsız kapı). Ayrıca: TUI `sidebar_toggle = <leader>b` bulundu (Alp'in sorusu; bilgi amaçlı).

## B) Patron (Şef) önerileri

| # | Öneri | Gerekçe | Durum |
|---|---|---|---|
| B1 | Yeni bir dosya: `knowledge/roadmap/ONERI-LISTESI.md` (bu listenin repo kopyası) → Alp `git pull` ile okuyabilsin, Doktor üzerine ekleyebilsin | Alp offline; tek kanal git | öneri |
| B2 | Her öneriye **ID + durum** verilsin; "yavaş yavaş yaparız" akışı bu sütunla yürüsün (öneri → onaylı → yapıldı) | takip kolaylığı | öneri |
| B3 | Öneriler **iki kovaya** ayrılsın: (i) saha deneyimi/ürün, (ii) güvenlik/denetim — Alp hangi kovadan başlayacağını seçsin | öncelik kararı Alp'te | öneri |
| B4 | Saha sabitleri (satellite `pub` dizini, NTP/AD değerleri, envanter yeri) **`AGENTS.md` şablonunda** örnek satır olarak yer alsın | her oturumda tekrar anlatılmasın | öneri |
| B5 | Ekran görüntüsü → öneri akışı kalıcı olsun: her görüntü geldiğinde Patron bu listenin A bölümüne satır ekler, Doktor C bölümüne kendi önerisini yazar | Görev #1 hattı | yapıldı |
| B6 | Doktor'un önerileri **koda yakın** olsun: hangi dosya/modül, tahmini zorluk, riskli mi (vendor/upstream'e gidiyor mu) | uygulanabilirlik | öneri |
| B7 | Upstream'e gidebilecek düzeltmeler ayrı işaretlensin (`upstream-uygun`) — "fork yok" ilkesi ile uyum | karar 2026-09-14 | öneri |
| B8 | Sürüm notu kuralı: her `yapıldı` maddesi `SURUM-NOTLARI.md`'de tek satır | izlenebilirlik | öneri |

## C) Doktor (Claude Code) önerileri

> Doktor dolduracak: her madde için **öneri · ilgili dosya/modül · tahmini zorluk · risk/upstream notu**.
> En az 8 madde; kod/derleme/izin/audit/test/doküman eksenlerinden en az 3'üne dağılsın.

Aşağıdaki maddeler mevcut kod/dokümanı okuyarak doğrulanmıştır (A/B'deki maddelerin tekrarı değil, onlara
teknik derinlik ekler ya da ayrı, doğrulanmış boşlukları hedefler).

| # | Öneri | Dosya/modül | Zorluk | Risk / upstream notu |
|---|---|---|---|---|
| C1 | **[derleme/ikili]** Derleme sonunda tek bir `bin/BUILD-MANIFEST.json` (sürüm, kanal, commit, ISO-tarih, ikili sha256, bun/node sürümü) yazılsın; `kur.sh kontrol` A5/T4'ü bu tek dosyadan okusun — şu an sürüm bilgisi yalnız `--version` çıktısından ve `kur.sh`'ın kendi `SURUM`/`KANAL` değişkenlerinden (kur.sh:57-67, 311-313) geliyor, ayrı bir doğrulanabilir kayıt yok | `packages/opencode/script/build.ts`, `kur.sh` (`kontrol()`, `kontrol_kurulum()`) | Düşük (D) | Vendor dosyasına (`build.ts`) enjekte alan eklemek upstream sync çakışma yüzeyini büyütür — değişiklik küçük, izole bir fonksiyonda tutulmalı. `upstream-uygun` değil (fork'a özel sürüm izleme ihtiyacı). |
| C2 | **[izin & politika]** `permission/evaluate.ts`'de bash izin eşleşmesi, komutta shell metakarakteri (`;`, `&&`, `\|\|`, `\|`, backtick, `$()`) varsa whitelist'i **bypass edip otomatik `ask`/`deny`'a düşsün** — A15/A9'un işaret ettiği "`ls; rm -rf …`" açığının kök nedeni burada; şu an eşleştirme tek komut varsayımıyla çalışıyor | `packages/opencode/src/permission/evaluate.ts`, `packages/opencode/src/permission/arity.ts` | Orta (O) | Davranış değişikliği geniş — bugün izinli sayılan bazı meşru bileşik komutlar (`cd x && npm test` gibi) "ask"a düşebilir; saha AGENTS.md'lerinde whitelist gözden geçirmesi gerekir. Genel güvenlik iyileştirmesi olduğundan `upstream-uygun`. |
| C3 | **[audit/log]** Faz 0'ın `engine/plugins/audit-log.ts`'i `prev_hash` zinciri yazıyor (AUDIT-FORMAT.md §3) ama zincirin **bütünlüğünü sahada doğrulayan bağımsız bir script yok** — `logrotate` (günlük, `compress`) sonrası sessiz bir kopma fark edilmeyebilir. `script/dogrula-audit-zinciri.sh` eklensin: `/var/log/ops-agent/audit.jsonl*` üzerinde `prev_hash` sırasını baştan sona doğrulasın, kopma varsa satır numarasıyla raporlasın | `engine/plugins/audit-log.ts`, `knowledge/policy/AUDIT-FORMAT.md`, yeni `script/dogrula-audit-zinciri.sh` | Orta (O) | Salt-okunur, sahaya yazma yapmıyor — düşük risk. Log yolu (`/var/log/ops-agent/...`) fork'a özel, `upstream-uygun` değil. |
| C4 | **[test/CI]** `packages/opencode/src/session/system.ts:104`'teki `.toSorted((a,b) => a.name.localeCompare(b.name))` çökme kalıbı (9ad34675c6'da düzeltildi, test eklendi) **`packages/opencode/src/skill/index.ts`'de 3 ayrı yerde aynı desenle tekrar ediyor** (298, 312, 328, 343 satırları) — malformed skill kaydı (isim alanı eksik) aynı sınıf çökmeye yol açabilir; bu dosyalara da savunmacı filtre + regresyon testi eklensin | `packages/opencode/src/skill/index.ts`, `packages/opencode/test/skill/*.test.ts` (yoksa oluştur) | Düşük (D) | Küçük, izole değişiklik; mevcut düzeltmenin deseni tekrarı — risk düşük. Genel sağlamlaştırma, `upstream-uygun`. |
| C5 | **[performans/bağlam yönetimi]** `KURUM_MAX_CONTEXT` (kur.sh:550-555) yalnız `kontrol` ekranında **gösteriliyor**, opencode'un kendi provider/model config'ine (context-limit hesaplayan katman) hiç yazılmıyor — A10'daki "context uyarısı" (%15/%17) muhtemelen uçtan gelen gerçek pencere yerine varsayılan/modeldev değeriyle hesaplanıyor. `kur.sh` bu değeri kurulumda `opencode.json`/model config'ine yazsın ki TUI'nin kendi yüzde hesabı doğru pencereyi kullansın | `kur.sh` (env → config yazımı), `packages/opencode/src/provider/provider.ts` | Orta (O) | Yanlış değer yazılırsa TUI'nin context uyarısı yanıltıcı olur (düşük ama gerçek risk) — kurulum sırasında tek satır doğrulama (`kontrol` çıktısındaki pencere ile config'teki değer eşleşmeli) eklenmeli. `upstream-uygun` değil (kurum-özel env). |
| C6 | **[izin & politika]** `AGENTS.md` şablonu (A8) yazılırken, alt-ajan (Task/Agent tool) çağrılarının `packages/opencode/src/agent/subagent-permissions.ts` üzerinden **üst ajanın izin setini miras alıp almadığı** açıkça belgelenmeli — aksi halde A9'daki "tehlikeli komut" politikası alt-ajan katmanında sessizce delinebilir. Şablona "alt-ajan izin devri" bölümü eklensin | `packages/opencode/src/agent/subagent-permissions.ts`, `AGENTS.md` şablonu (A8 kapsamı) | Düşük (D) | Dokümantasyon + doğrulama; kod değişikliği gerektirmiyorsa risksiz. Kod değişikliği gerekiyorsa önce mevcut davranış test edilip netleştirilmeli. |
| C7 | **[dokümantasyon]** `knowledge/incidents/` ve `knowledge/architecture/decisions/`'ın aksine `knowledge/roadmap/` dizininde bir `INDEX.md` yok (`backlog.md`/`ideas.md`/`planned-features.md`/bu dosya arasında gezinme tek tek `README.md` satırına bağlı) — `knowledge/incidents/INDEX.md` deseniyle uyumlu kısa bir `knowledge/roadmap/INDEX.md` eklensin | `knowledge/roadmap/INDEX.md` (yeni), `knowledge/roadmap/README.md` | Düşük (D) | Salt dokümantasyon, davranış değişikliği yok. Repo-özel düzen, `upstream-uygun` değil. |
| C8 | **[derleme/ikili]** `script/smoke-ikili.sh` şu an yalnız `a.name` çökmesini (2026-09-22 olayı) hedefliyor; A4/A14 ile birlikte genişletilirken **ikinci bir gerçek regresyon senaryosu** eklenmeli: `kur.sh derle` sonrası `--version` çıktısının `SURUM`/`KANAL` ile eşleştiğini doğrulamak (C1'deki manifest hazır olursa ona karşı) — böylece duman testi yalnız "çökmüyor" değil "doğru sürümü basıyor" da doğrular | `script/smoke-ikili.sh` | Düşük (D) | Mevcut testin küçük bir uzantısı, geriye dönük uyumlu. `upstream-uygun` değil (kurum sürüm şeması fork'a özel). |
| C9 | **[test/CI]** A14'teki CI önerisi somutlaştırılırsa: mevcut `.github/workflows/*.yml` tamamı upstream'den geliyor (typecheck.yml, test.yml, vb.) — fork'un kendi minimal doğrulaması (`bash -n kur.sh`, `shellcheck kur.sh`, `script/smoke-ikili.sh`) **ayrı, açıkça "fork-özel" işaretli bir dosyada** olmalı (örn. `.github/workflows/fork-kur-duman.yml`) ki upstream sync PR'larında yanlışlıkla silinmesin/upstream değişikliğiyle çakışmasın | yeni `.github/workflows/fork-kur-duman.yml` | Orta (O) | CI runner'da bun/node kurulumu + derleme süresi maliyeti var; offline kısıtı sahaya özgü, CI'a uygulanmaz. Dosya başına açık "fork-özel, upstream sync'te dokunma" yorumu şart. `upstream-uygun` değil. |
| C10 | **[kurulum/doküman — A17 için]** Saha sabitleri `AGENTS.md`'ye **yazılmamalı**: `kur.sh` adım "2/4" bu dosyayı şablonlamıyor, `cp -f "$KOK/engine/AGENTS.md" "$HOME/.config/opencode/AGENTS.md"` ile **birebir üzerine yazıyor** (kur.sh:482-484) — sahada elle eklenen her satır bir sonraki `./kur.sh` koşusunda silinir. Bunun yerine: (a) `env.example`'a opsiyonel `SAHA_*` değişkenleri (AD/NS, NTP havuzu, satellite `pub` yolu, envanter dizini), (b) `kur.sh` bunlardan `~/.config/opencode/saha-sabitleri.md` üretsin (`umask 077` deseni — kur.sh:1087 / bulgu #8, kur.sh:581), (c) `engine/opencode.json`'a `"instructions": ["~/.config/opencode/saha-sabitleri.md"]` eklensin. Doğrulandı: `config.instructions` `~/` ve mutlak yolu çözüp sistem promptuna ekliyor (`session/instruction.ts:135-150`, `156-169`) ve **dosya yoksa glob boş dönüp sessizce atlanıyor** (aynı yerde `Effect.catch`) — yani sabit tanımlamayan makinede hata üretmez. Maskeleme korunur: değerler yalnız `env` (`.gitignore:40` → `/env`) ve `$HOME` içinde kalır, repoda yalnız yer tutucu durur; `kontrol` ekranında A1 ile aynı `maskele()` yolundan (kur.sh:1501-1512) basılır | `kur.sh` (adım 2/4, satır 480-484 + opencode.json üreten PY bloğu 558+), `engine/opencode.json`, `env.example`, `engine/AGENTS.md` (yalnız yer tutucu) | Orta (O) | Bağlam bütçesi riski: uç penceresi `engine/opencode.json`'da `limit.context: 16384` — bu dosya her isteğe giriyor, **≤15 satır** tutulmalı. Kurum-özel env akışı, `upstream-uygun` **değil**. `02-kur.sh` diye ikinci bir kurulum betiği repoda **yok** (arandı, tek betik `kur.sh`). |
| C11 | **[politika/prompt — A18 için]** Önce tespit: gözlenen davranış **hata değil, mevcut politikanın sonucu** — `engine/AGENTS.md:74-79` ("Tek adım disiplini") açıkça *"görev bitmediyse bile … kullanıcının onayını bekle"* ve *"Emin değilsen çağırma; sor"* diyor. Yani A18 öncelikle bir **`AGENTS.md` politika bloğu** işi, çekirdek işi değil. İkinci tespit: önerinin mekanizması bugün **kapalı** — `engine/opencode.json`'da `"todowrite": "deny"` ve `"edit": "ask"`; ajanın eksik bir öğeyi kaydetmek için bloklamayan **hiçbir** yolu yok (`eksikler.md` yazmak da izin sorar = yine bloke). Öneri: (a) `engine/AGENTS.md`'ye "Eksik bilgide bloklama" bloğu — çözülemeyen öğeyi **yanıt metninde** "Açık kalanlar" başlığı altında topla (dosya yazımı yok → izin kapısına takılmaz), kalanla devam et; "önce sor" kuralı yalnız **yıkıcı/geri-dönülemez** adımlara daraltılsın (Güvenlik bölümüyle tutarlı). (b) Kalıcı dosya isteniyorsa `todowrite: "deny"` → `"allow"` ayrı bir karar olarak ölçülmeli: `task` aynı gerekçeyle (16k pencerede araç şemasını küçük tutmak) kapatılmıştı (engine/AGENTS.md:54-59) — todowrite şemasının pencere maliyeti **doğrulanmadı**, açılmadan önce ölçülmeli. Çekirdek değişikliği **önerilmiyor**: genel "ask the user" talimatı vendor prompt'unda (`session/prompt/default.txt:75`) ve model başına 15 ayrı `prompt/*.txt` dosyasında — yama her upstream sync'te çakışır | `engine/AGENTS.md` (yeni politika bloğu), `engine/opencode.json` (`permission.todowrite`), — karşılaştırma: `packages/opencode/src/session/prompt/default.txt` (dokunulmaması önerilir) | Düşük (D) doküman / Orta (O) todowrite açılırsa | Doküman kısmı risksiz ve geri alınabilir; asıl risk politikanın ters yöne kaçması (model yıkıcı adımı da "açık kalanlar"a yazıp kendi kendine devam etmesi) → blok daraltma **açıkça yıkıcı komut listesine** bağlanmalı. Kurum davranış kuralı, `upstream-uygun` **değil**; todowrite değişikliği ise yalnız fork config'i. |
| C12 | **[politika/doküman — A19 için]** CN kuralı `engine/AGENTS.md`'de **"Güvenlik ve sınırlar"** bölümüne (satır 36-49) yeni bir alt blok olarak yazılmalı — ayrı bir dosyaya değil, çünkü `kur.sh` bu dosyayı sahaya `cp -f` ile birebir kopyalar (C10 bulgusu: elle eklenen satır bir sonraki koşuda silinir). Blok **≤10 satır** olmalı: uç penceresi `engine/opencode.json`'da `limit.context: 16384` ve `AGENTS.md` her isteğe giriyor. Yazılacak üç ifade: (1) **salt-okunur serbest** — ssh/kubectl ile okuma, `Read`/`Glob`/`Grep`; bu zaten AGENTS.md satır 7-15 (kural hiyerarşisi) ve 17-34'te (SSH erişimi) tanımlı, CN bloğu onlara **atıf yapmalı**, kuralı ikinci kez yazmamalı. (2) **`<envanter>`de mutasyon = CN + kapsam** — playbook adı, hedef host listesi, beklenen değişiklik yanıtta CN ile birlikte yazılmadan çalıştırma yok. (3) **Yeni kurulan makine istisnası** — henüz canlı envantere girmemiş makinede CN gerekmez, ama ajan bunu açıkça beyan eder. Ek: CN yoksa **tüm tur durmaz** (A18 ile hizalı) — madde "Açık kalanlar"a yazılır, salt-okunur kısımla devam edilir. `PERMISSION-MATRIX.md` ile hizalanma: §3 tablosuna (satır 104-112) bir satır eklenmeli — *"canlı envanterde mutasyon playbook'u"* → yeri **hem** `AGENTS.md` kuralı **hem** kapı (C13); aynı dosyanın "kısa kural"ı (satır 114-117) zaten *"geri dönüşü olmayan her şey kapıya bağlanır"* diyor. **Kritik uyarı bloğa yazılmalı:** `AGENTS.md:46-49` açıkça *"İzinler bu dosyada TANIMLANMAZ"* diyor — CN bloğu bir **davranış kuralıdır**, izin kapısı değil; metin bunu kendi içinde söylemeli ki model "AGENTS.md beni engeller" varsayımına düşmesin | `engine/AGENTS.md` (§Güvenlik ve sınırlar), `knowledge/policy/PERMISSION-MATRIX.md` §3, — bağlam: `kur.sh` adım 2/4 (`cp -f`) | Düşük (D) | Salt doküman, geri alınabilir. Tek gerçek risk **yanlış güven**: kural yazılınca sorun çözülmüş sanılır — A20 tam olarak bu; bu yüzden C12 **tek başına yeterli değildir**, C13 ile birlikte planlanmalı. Kurum süreç kuralı, `upstream-uygun` **değil**. |
| C13 | **[izin & audit — A20 için, kritik]** Önce tespit: kapı **eksik değil, yetersiz**. `engine/opencode.json:97` zaten `"ansible-playbook*": "ask"` içeriyor — yani playbook çalıştırma onaya düşer, ama onay bir tuş vuruşudur, CN alanı yoktur. Asıl açık **kodla doğrulandı**: kullanıcı bir kez **"always"** derse `permission/index.ts:145-151` oturuma bir `allow` kuralı ekler ve desen `shell.ts:409` → `BashArity.prefix(tokens)` ile üretilir; `ansible-playbook` **ARITY sözlüğünde yok** (`permission/arity.ts:24-161`) → prefix 1 token → eklenen desen **`ansible-playbook *`**. `Wildcard.match` (`core/util/wildcard.ts:3-14`) `*`'ı `.*` + `s` bayrağıyla açtığından bu desen o oturumdaki **her** ansible komutunu kapsar; dahası `evaluate()` (`permission/index.ts:28-38`) `findLast` kullanıp `approved`'ı ruleset'ten **sonra** koyduğu için oturum onayı config'teki `deny` desenlerini de **ezer**. Yani `--check`'li zararsız bir koşuya verilen tek "always", aynı oturumdaki `03-ipa` gibi mutasyonları sessizce açar — A20'nin en olası mekanizması budur (sahada yeniden üretilmedi → **doğrulanmadı**, hangi yolun kullanıldığı repo dışı). Üç seçeneğin değerlendirmesi: **(a) izin whitelist'i** — tek başına **yetmez**: `Wildcard` yalnız `*`/`?` destekliyor, olumsuzlama (`--check` *yokken*) ifade **edilemez**, üstelik yukarıdaki "always" ezmesi deny desenlerini etkisizleştirir. **(b) hook ile `ask`'a düşürme** — bugün **mümkün değil**: `permission.ask` hook'u plugin API'sinde tanımlı (`packages/plugin/src/index.ts:261`, çıktı `{status: "ask"\|"deny"\|"allow"}`) ama motor bu adı **hiç tetiklemiyor** (repo genelinde yalnız tanım bulundu; çalışma zamanında olan şey `permission.asked` **olayı**, hook değil). Bunu çalışır hale getirmek `permission/index.ts` `ask()` içine tek bir `plugin.trigger` çağrısı demek — küçük, genel ve **`upstream-uygun`** olan parça budur. **(c) `tool.execute.before` kapısı** — **bugün çalışır** ve sahaya minimum riskle giren seçenek: `engine/plugins/` altına yeni `degisiklik-kapisi.ts` (README'deki `guvenlik-kapisi` adayı, satır 54); hook `item.execute`'tan **önce** tetikleniyor (`session/tools.ts:106-112`) ve `plugin/index.ts:284-297` her hook'u `await` ettiğinden reddeden hook çağrıyı iptal eder (regresyon testi: `test/tool/code-mode.test.ts:429`). Kural: `args.command` içinde `ansible-playbook` var **ve** `--check`/`-C` yok **ve** ortamda CN yoksa → reddet. Sınır: hook yalnız **sert red** verebilir (ask'a düşüremez, bkz. (b)) → tıkanmayı önlemek için CN bir kaçış yolu ile verilmeli (`OPS_AGENT_CN=<cn>`; A19'un yeni-makine istisnası için `OPS_AGENT_CN=YENI-KURULUM`). **Audit ayağı:** `audit-log.ts:281-291` her kayda `policyDecision: "allow"` **sabit** yazıyor — "onaysız mutasyon" ayrı bir durum olarak görünmüyor; ayrıca `tool.execute.after` yalnız araç **çalıştıysa** tetiklendiğinden reddedilen çağrı ancak `session.idle` bayat-kayıt yolundan (satır 305-324, 5 dk eşiği) log'a düşer. Minimum iş: kapı plugin'i kendi `denied` kaydını aynı `prev_hash` zincirine yazsın ve kayda `cn` alanı eklensin (`AUDIT-FORMAT.md` güncellenir). **Öneri sırası: (c) şimdi → (b) upstream'e → (a) yalnız destek.** Bağlam: T7 (`queue/opencode-GELISTIRME-KUYRUGU.md`, `[ONAY-BEKLIYOR]`) ve `knowledge/roadmap/PHASE0-ACCEPTANCE.md` | yeni `engine/plugins/degisiklik-kapisi.ts`, `engine/plugins/audit-log.ts`, `engine/opencode.json` (`permission.bash`), `knowledge/policy/AUDIT-FORMAT.md`; — çekirdek (yalnız (b) için): `packages/opencode/src/permission/index.ts` `ask()` | Orta-Yüksek (OY) | **Fail-closed bir kapı**: yanlış kurulursa meşru bakım işini de durdurur → önce `--check`'li koşularda gözlem modunda (yalnız audit kaydı, red yok) çalıştırılmalı. (c) fork'a özel, `upstream-uygun` **değil**; (b)'deki `permission.ask` tetikleme eksiği genel bir boşluktur, ayrı ve küçük bir yama olarak **`upstream-uygun`**. Not: C2'deki bileşik-komut endişesi bu madde için **geçerli değil** — `shell.ts:378-411` her alt komutu ayrı düğüm olarak tarayıp desen üretiyor, yani `cd x && ansible-playbook …` da eşleşir. |
| C14 | **[doküman/kurulum — A21 için · kısmen YAPILDI]** Kanıt: spinner çerçeveleri **braille** (`packages/tui/src/component/spinner.tsx:10` — `⠋⠙⠹…`, U+280B…); locale/charset/font zincirinden biri UTF-8 değilse kutu/`?` çıkar. **Bu koşuda uygulanan iki küçük adım:** (1) `kur.sh` `kontrol_kurulum()`'a **10. kontrol** eklendi (`kur.sh:997-1009`) — `LC_ALL`→`LC_CTYPE`→`LANG` zinciri `*.UTF-8` **değilse** tek `locale !` satırı + `--ayrintili` altında 3 düzeltme satırı basar; UTF-8 ise **hiç satır basmaz**, yani sağlam kurulumda rapor uzunluğu değişmez (A2/≤29 satır korunur — ölçüldü: 17 satır). Davranış değişikliği yok, yalnız uyarı. (2) `NASIL-CALISTIRILIR.md` saha kurulum bölümüne **"Terminal görüntüsü — glifler kutu/`?` çıkıyorsa"** başlığı: sunucu locale + MobaXterm Font + Charset tablosu, `Ctrl+P` → *Disable animations*. **Doğrulanan ince nokta:** bu anahtar bir `tui.json` alanı **değil** — `Ctrl+P` komutu `kv.set("animations_enabled", …)` çağırıyor (`packages/tui/src/app.tsx:894-899`) ve KV `<state>/kv.json`'a yazılıyor (`packages/tui/src/context/kv.tsx:16, 53-60`); `TuiConfig.Info` şemasında böyle bir alan yok (`packages/tui/src/config/index.tsx:61-75`). Yani ayar **kalıcı** ve `./kur.sh` onu ezmez (betikte `tui` geçmiyor). **Uygulanmayan öneri (küçük, upstream-uygun):** animasyon kapalıyken kullanılan yedek glifler de Unicode — `⋯` (`spinner.tsx:17`) ve `[⋯]` (`packages/tui/src/component/prompt/index.tsx:1524`); ASCII-güvenli hâle getirmek **2 satırlık** değişiklik (`⋯`→`...`, `[⋯]`→`[...]`) ve locale bozuk makinede "Disable animations"ı gerçekten tam çözüm yapar. **13:18 ekranı teyidi:** metin ve Türkçe karakterler düzeldikten sonra kalan 4 kutu yalnız **animasyon glifleri** bölgesinde kaldı (`Build` öncesi ×2, ilerleme çubuğu yanı ×2) — sahada tam sıfırlama için ya `animations_enabled=false` (`Ctrl+P` → *Disable animations*) ya da braille/blok kapsayan tam Unicode font gerekir; bu ekran yukarıdaki `kur.sh` locale uyarısıyla **uyumlu**, çünkü locale UTF-8'e gelince metin düzeldi ve kalan eksik **font** katmanında | **yapıldı:** `kur.sh` (`kontrol_kurulum()` #10), `NASIL-CALISTIRILIR.md`; **öneri:** `packages/tui/src/component/spinner.tsx:17`, `packages/tui/src/component/prompt/index.tsx:1524` | Düşük (D) | Uygulanan kısım salt uyarı + doküman → risksiz, geri alınabilir. ASCII yedek glif değişikliği vendor (`packages/tui`) dosyasına dokunur ama 2 karakter dizisi — çakışma yüzeyi minimal ve genel bir erişilebilirlik iyileştirmesi olduğu için **`upstream-uygun`**. |
| C15 | **[kod/hata bildirimi — A22 için]** (a) **Sessiz yutma, kanıtlı:** `write()` (`packages/tui/src/clipboard.ts:121-125`) sırayla `writeOsc52` + seçilen arka uç çağırıyor; **üç arka ucun üçü de** hatayı yutuyor — `osascript` (satır 106), yerel komut `xclip/xsel/wl-copy` (satır 111), `clipboardy` (satır 116), hepsi `.catch(() => undefined)`. Üstüne `writeOsc52` TTY yoksa sessizce `return` ediyor (satır 24) ve OSC 52'nin **terminal tarafından kabul edilip edilmediği zaten doğrulanamaz** (tek yönlü escape dizisi). Saha makinesinde `copyCommand()` (satır 77-95) Linux'ta `wl-copy`/`xclip`/`xsel` bulamayınca `undefined` dönüp `clipboardy`'ye düşüyor, o da aynı X araçlarını arayıp başarısız oluyor → `write()` **her hâlükârda `resolve`** ediyor. Bu yüzden `selection.ts:37-40`'taki `.then(() => toast.show({… "Copied to clipboard" …}))` koşulsuz "başarılı" basıyor; `.catch(toast.error)` bu yolda **ölü kod**. **Kapsam A22'de yazılandan geniş:** aynı yanıltıcı toast **4 yerde** — `util/selection.ts:39`, `app.tsx:442`, `component/dialog-provider.tsx:258`, `ui/dialog.tsx:192` (ayrıca `component/error-component.tsx:48` koşulsuz `setCopied(true)`). Bu yüzden **çağrı yerini değil kaynağı** düzelt. **En küçük yama:** `write()` imzası `Promise<void>` → `Promise<boolean>`; her `.catch(() => undefined)` → `.catch(() => false)`, başarı yolunda `true`; `writeOsc52` yazdıysa `true` döner ama bu **"denendi, doğrulanamadı"** anlamındadır — dolayısıyla `write()` `{ native: boolean; osc: boolean }` benzeri iki bilgi döndürmeli ki toast dürüst olsun: yerel arka uç başarılıysa *"Copied to clipboard"*, yalnız OSC 52 denendiyse *"Kopyalama denendi (OSC 52) — terminal desteklemiyorsa pano boş kalır"*, hiçbiri olmadıysa **`variant: "warning"`** ile *"Kopyalanamadi — pano arka ucu yok (xclip/xsel/wl-copy)"*. Toplam ~15 satır, 5 çağrı yeri tek tip. (b) **`mouse:false` yolu:** alan `TuiConfig.Info` şemasında tanımlı (`packages/tui/src/config/index.tsx:74`), `resolve()` varsayılanı `true` (satır 128) ve tek tüketici `packages/tui/src/app.tsx:202` → `useMouse: !Flag.OPENCODE_DISABLE_MOUSE && input.config.mouse`. Yani `false` verince TUI fare yakalamayı hiç açmaz, seçimi terminal yapar → MobaXterm copy-on-select çalışır. **Hangi dosya (ConfigPaths ile teyit edildi):** yükleme sırası `packages/opencode/src/config/tui.ts` `loadState()` — 1) global `~/.config/opencode/tui.json`\|`.jsonc` (satır 184 + `config/paths.ts:43-45`; `Global.Path.config` = `$XDG_CONFIG_HOME/opencode`, `packages/core/src/global.ts:13, 26`), 2) `OPENCODE_TUI_CONFIG` (satır 189-193), 3) cwd'den yukarı yürüyen proje `tui.json`/`tui.jsonc` — kök-önce uygulanır, **en yakın kazanır** (satır 176 + `paths.ts:10-21`), 4) `.opencode/tui.json` ve `OPENCODE_CONFIG_DIR` (satır 203-210, **en yüksek öncelik**). **Saha için önerilen yer: global `~/.config/opencode/tui.json`** — çünkü `kur.sh` bu dosyaya **hiç dokunmuyor** (betikte `tui` geçmiyor), oysa `AGENTS.md`/`opencode.json` her koşuda `cp -f`/yeniden üretiliyor (C10 bulgusu), yani proje içi kopya sahada silinme riski taşır. İçerik: `{"mouse": false}` (şema `{"tui": {…}}` sarmalını da kabul ediyor — `tui.ts:55-69` `normalize()`). **Tek satırlık saha testi (dosya yazmadan):** `OPENCODE_DISABLE_MOUSE=1 opencode` (`packages/core/src/flag/flag.ts:30`) | `packages/tui/src/clipboard.ts` (`write`/`getCopyMethod`/`writeOsc52`), `packages/tui/src/util/selection.ts:37-40`, `app.tsx:442`, `component/dialog-provider.tsx:258`, `ui/dialog.tsx:192`; — belge: `NASIL-CALISTIRILIR.md` (`tui.json` + `mouse:false`) | Düşük-Orta (DO) | (a) **`upstream-uygun`** — "başarısız kopyalamayı başarılı gösterme" genel bir hata; yama küçük ve davranışı yalnız **bildirim** düzeyinde değiştiriyor (kopyalama mantığı aynı kalır). Risk: `OSC 52`'yi destekleyen terminallerde gereksiz uyarı çıkmasın diye "denendi" durumu **ayrı** tutulmalı — tek boolean'a indirgemek yeni bir yalana yol açar. (b) salt config + doküman, risksiz; `mouse:false` fareyle kaydırmayı da kapatır (kullanıcıya söylenmeli). Büyük refactor **önerilmiyor**. |
| C18 | **[güvenlik/maskeleme — YAPILDI (T12)]** T9 maskeleme taraması **host/IP/URL** odaklıydı; bu yüzden şablonda kalan **gerçek iç model yolu** gözden kaçtı (`env.example` `MODEL_ID`). Kural genişletildi: maskeleme yalnız ad/adres değil, **kuruma özgü mutlak yollar ve iç dizin kalıpları** için de geçerlidir (`/data/…`, `/<uygulama>/…`, `/<log-dizini>…`, `models--<saglayici>--<model>`). Uygulanan: (a) `env.example` `MODEL_ID="<model-kimligi>"` — yorum gerçek adın **uçtan** (`/v1/models`) birebir kopyalanacağını söyler, biçimi yalnız **genel** kalıpla anlatır; aynı gerçek değer 3 yerde daha duruyordu (`engine/opencode.json` `id` → `MODEL_ID_YER_TUTUCU`, `NASIL-CALISTIRILIR.md` env.local örneği, `kur.sh` env kurtarma ipucu) ve hepsi yer tutucuya çevrildi — yoksa "tarama 0" sağlanamazdı. (b) **Şablon muhafızı** yer tutucuyu dolu değerden ayırıyor: `kur.sh` `kurulum` kapısı `MODEL_ID`'de `<` görürse durur, `kontrol_kurulum()` env satırı `sablon degeri duruyor (KURUM_ENDPOINT + MODEL_ID=<...>)` der, uç teşhisi `MODEL_SABLON` ile aynı kapıya düşer. **İnce nokta (doğrulandı):** `env` kabukta `.` ile okunuyor → `<…>` **tırnaksız** yazılırsa `<` yönlendirme sayılır ve dosya bozulur; bu yüzden `MODEL_ID="<model-kimligi>"`. (c) Kural + **tekrarlanabilir tarama komutu** maskelemenin yaşadığı yere yazıldı: `knowledge/policy/THREAT-MODEL.md` **§6** (`git ls-files` ile yalnız izlenen dosyalar, kapsam fork katmanı; `packages/`+`bun.lock` dışarıda çünkü upstream'de `/data/` ve `10.x` sürümleri yüzlerce yanlış pozitif üretiyor). Taramanın **kendisi de** maskeye tabi: desene gerçek host/yol yazılmaz, yeni kalıp **genel** biçimiyle eklenir | `env.example`, `engine/opencode.json`, `NASIL-CALISTIRILIR.md`, `kur.sh` (3 muhafız), `knowledge/policy/THREAT-MODEL.md` §6 | Düşük (D) | Şablon + doküman; davranış yalnız **daha erken hata** yönünde değişti (yer tutucuyla kurulum artık sessizce geçmiyor). Kanıt: `bash -n kur.sh` temiz, `script/duman-kontrol-rapor.sh` **22/22**, şablondan üretilen `env` ile `./kur.sh kontrol` hâlâ "sablon" diyor (çıkış 2), doldurulmuş `env` ile `env ✓`, tarama **0**. Kurum hijyen kuralı, `upstream-uygun` **değil**. |

## D) Uygulama notu

- Alp: *"ondan sonra yavaş yavaş yaparız"* → tek seferde 1-2 madde; her madde kanıt + commit + push.
- `yapıldı` işaretlenen maddeler bu dosyada kalır (silinmez), sürüm notuna link verilir.
- Son güncelleme: 2026-09-23 (**12:27/13:18 ekranı** — A21 kanıtı Patron'dan senkronlandı: MobaXterm font listesinde braille yok + metin/Türkçe düzeldi, kalan 4 kutu yalnız animasyon glifleri; C14'e teyit cümlesi Doktor).
  Kaynakta ayrı bir "12:20/13:18 eki" notu **yok** (A/B birebir kopyalandığı için buraya da eklenmedi); bu ekranların izi A21/A22'nin `Kaynak` hücresindedir.
